# 坑位图鉴 UI/UX 迭代 · 交接文档

> 日期：2026-08-26 ｜ 分支：`ui-redesign-20260826`（commit `e21937d`）｜ 基线：`main` @ `323d4ef`（tag `ui-baseline-20260826`）
> 本次只动前端 `gaj/web/static/`，后端接口与数据结构零改动；数据文件（`data/`）未受影响。

---

## 一、新界面结构

```
┌──────────────────────────────────────────────────────────────┐
│ 顶栏: 品牌 🎯 坑位图鉴 │ 统计胶囊(可点击→直达筛选) │ 批量* 规则打分 │
├──────────┬───────────────────────────────────────────────────┤
│ 侧边导航  │ 视图内容区 (config-panel / guide-panel /           │
│ 工作台    │   observatory-panel / main 统一为 view-host 子项)   │
│ ·职位列表 │                                                   │
│ ·公司图鉴 │                                                   │
│ 市场  │                                                   │
│ ·市场观察│                                                   │
│ ·行业观察 │ ← 规划占位 (禁用态 + 「规划」徽标, TODO#3)          │
│ 配置      │                                                   │
│ ·规则配置 │                                                   │
│ ·主简历  │                                                   │
├──────────┴───────────────────────────────────────────────────┤
│ 底部: 实时日志 (可折叠, 保留)                                  │
└──────────────────────────────────────────────────────────────┘
* 批量按钮仅在职位列表视图显示
```

### 导航规则
- **所有视图 1 次点击可达**：视图状态单一真相源仍是 `$store.core.view`，侧边导航项直接 `switchView`
- **统计胶囊 = 快捷筛选**：`store.openJobsWithFilter(preset)` 切到职位视图并预置筛选（收藏/已忽略/未打分/AI分/规则分/全部重置）；`jobsPanel` 监听 `gaj:jobs-filters` 事件合并筛选
- **跨视图链路**（沿用）：`store.openJob(jobId)` / `store.openCompany(brandId)` + window 事件 `gaj:open-job` / `gaj:open-company`

### 视图内结构
- 职位列表：搜索 + 筛选行（城市/状态/打分/收藏/忽略带标签，排序下拉分组 综合/薪资/时间/数据/其它）→ 列表 → 分页
- 职位详情：节导航（基础信息/市场参考*/规则打分/人工调分/AI打分/岗位描述/公司简介）+ 标题操作栏 + 滚动 sticky 摘要条（操作与标题栏一致，仅省供应商选择）
- 公司图鉴：两行工具栏（进度+想去+三榜 / 三模式+搜索+筛选）→ 卡片墙/象限图/对比；公司抽屉新增 **市场数据** 区块*
- 市场观察：统一 `.tabs` 组件四 Tab（薪资定价/区域热力/信号雷达/技能热度）+ 下钻抽屉
- 规则配置 / 主简历：统一 `.tabs` 组件（含「关闭」返回职位列表）

> *TODO#1/#2/#4 已实现的落位项（详见第三节）

## 二、样式变量与公共组件位置

| 文件 | 职责 |
|---|---|
| `gaj/web/static/styles/base.css` | **设计令牌唯一来源**：`--gaj-*` 全部颜色/字号/间距/圆角/阴影/焦点环。本次微调：`--gaj-foreground-3` #5C6573→#677288（对比度），新增 `--gaj-focus-ring` |
| `gaj/web/static/styles/components.css` | **新增公共组件层**：全局 `:focus-visible` 焦点环、`.tabs/.tab`（下划线式 Tab）、`.toolbar/.toolbar-row`（两行工具栏）、`.stat-card`（统计卡）、`.state-empty/.state-loading`（空态/加载态）、`.spin`（旋转图标动画） |
| `gaj/web/static/style.css` | 外壳：`.app-shell` / `.header` / `.sidebar`（分组导航）/ `.view-host` / `.log-panel` / `.toast` / 按钮三态 / `.stat-pill` 可点击变体 |
| `gaj/web/static/styles/<view>.css` | 各视图专属样式（沿用命名，未重构） |

**公共组件使用约定**：新增页面优先用 components.css 里的积木（tabs/toolbar/stat-card/state-empty），不要另写一套；改视觉先改 base.css 令牌，再改组件层，最后才是视图层。

## 三、TODO.md 落位状态（本次迭代）

| # | 需求 | 状态 | 位置 |
|---|---|---|---|
| 1 | 岗位详情打开公司详情抽屉 | ✅ **已实现** | 职位详情「基础信息」公司项 + 「公司简介」卡头均有「查看公司图鉴」按钮 → `store.openCompany` |
| 2 | 公司图表与市场观察融合 | ✅ **已实现（轻量版）** | 公司抽屉新增「市场数据」区块；观察台三处公司入口（红旗榜/区域公司列表/岗位详情公司名）统一复用全局公司抽屉，就地打开不切视图 |
| 3 | 行业观察 + 下钻 + 政策分析 | 🚧 **规划占位** | 侧边导航「市场」组「行业观察」禁用项（带「规划」徽标）。实现时：启用该导航项 + 新建 `views/industry.html/js/css` 三件套，挂载点加在 `index.html` 的 `view-host` 内；聚合数据需新增后端接口（超出本次"只动前端"范围） |
| 4 | 岗位/公司详情展示市场关键指标 | ✅ **已实现（轻量版）** | 职位详情「市场参考」区块：行业薪资中位/全市场中位/市场均值/本岗薪资 + 高于/低于对比徽标。数据来自 `/api/observatory/salary`，客户端会话缓存 10 分钟（`store.ensureMarket` / `store.marketForIndustry`） |
| 5 | 市场数据优化规则与画像 | 🕓 **入口保留（开关控制）** | 规则配置「AI 规则矫正」卡片已存在，`views/config.js` 第 18 行 `showCalibration: false` 翻 true 即恢复入口；后端能力完整（`/api/config/calibrate*`） |

**注意**：市场参考/市场数据区块依赖观察台 salary 聚合的 `by_industry` Top10（样本≥2 的行业才出现）；不在 Top10 的行业区块自动隐藏，属预期行为。

## 四、后续叠加新功能的做法

1. **加导航项**：`index.html` 侧边栏对应分组内加 `<button class="sidebar-item">`，`@click="$store.core.switchView('xxx')"`；`store.js` 不需要改（视图路由是通用字符串）
2. **加视图岛**：按现有模式三件套——`views/xxx.html`（模板）+ `views/xxx.js`（`Alpine.data('xxxPanel')`）+ `styles/xxx.css`；挂载点插在 `index.html` 的 `view-host` 内（`data-tpl="xxx"`），loader 自动加载；CSS/JS 在 `index.html` 头部按序引用并把 `?v=` 版本号 +1（所有静态资源引用统一升版本号防缓存）
3. **加全局动作**：放顶栏 `.actions`；仅某视图可见的用 `x-show="$store.core.view === 'xxx'"`
4. **统计胶囊**：`index.html` 加 `<button class="stat-pill">`，`@click="$store.core.openJobsWithFilter({...})"`，preset 键名与 `jobsPanel.filters` 一致（search/city/status/scored/favorite/ignored/sort）
5. **改视觉**：令牌 → components.css → 视图 css，从上往下改，不要反向硬编码

## 五、验证记录（2026-08-26）

| 项 | 结果 |
|---|---|
| 服务启动 | `python3 -m gaj web` → http://127.0.0.1:8765 200 OK |
| 数据完整性 | 258 职位 / 258 规则分 / 76 AI 分 / 1 收藏 / 95 忽略，改造前后一致 |
| 统计胶囊直达 | 收藏→1 条、已忽略→95 条、未打分→0 条、重置→258 条（全部真实数据） |
| 公司跳转 | 职位详情 → 公司抽屉（帆软软件）打开正常 |
| 市场参考区块 | 计算机软件 行业 35 样本 / 中位 27.95 万/年 / 本岗 34.2 万/年 →「高于行业中位」 |
| 公司市场数据区块 | 5 岗 / 区间 18-49 万 / 均薪 30.68 万 vs 行业中位 27.95 万 →「高于」 |
| 边界宽度 | 1024/1152/1280/1440/1536/1600/1920/2400 × 5 视图全部无横向溢出 |
| 缩放模拟 | 80%（1600/1152）与 125%（1024/1536）视口均通过 |
| 空态/长文本 | 未打分空态正常；详情长 JD 滚动到底无横向溢出 |
| CRUD 回归 | 收藏 1→2→1 往返、忽略 移除/找回/取消 往返，数据恢复基线 |
| 控制台错误 | 全交互路径 0 异常（Alpine 表达式、网络、渲染） |
| 截图对比 | `docs/ui-redesign/assets/baseline/`（改前 7 张） vs `assets/after/`（改后 13 张）——⚠️ 该 `docs/ui-redesign/assets/` 资源已随归档不存在，属既存死链，仅作记录 |

## 六、回滚

```bash
git switch main          # 一步回到改造前 (main @ 323d4ef)
git tag ui-baseline-20260826   # 基线标记仍在
```
如保留改造但想重来：`git switch -c ui-redesign-v2 ui-redesign-20260826` 继续迭代。`data/` 不在版本控制内且本次未触碰。
