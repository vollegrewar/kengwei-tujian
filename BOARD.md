# 坑位图鉴（GAJ）工程看板 · BOARD.md

> **人和 agent 共用的工程操作台。** 顶部两区（当前优先队列 / 等你动手）是接下来要动的活，
> 往下是分组 backlog 与归档。本文件取代粗糙的 `TODO.md`（旧条目已归并到下方"方向/归档"区）。
>
> 用法约定：**安排、开工、完成一项就更新本文件一次**；别把它当永久 TODO，做完即下沉归档。
> 优先级沿用 P0/P1/P2/P3：P0 影响正确性/数据，P1 影响可用性/防丢，P2 一般优化，P3 规模大了再说。

---

## ⏳ 当前优先队列（下次动手从这里挑）

| 主题 | 优先级 | 卡在哪 / 状态 | 详见 |
|---|---|---|---|
| 杭州口径续采（断在第 17 页） | P1 | 09-19 采集被 BOSS 封禁（code=32）中断，解封后重跑 `crawl` 自动从 17 页续采 | §更新日志 2026-09-19 |
| SQLite 公司分公式去重 | P0 | 未开工，改动小收益高 | [db-sqlite-refactor-plan.md](docs/db-sqlite-refactor-plan.md#p0--公司分公式去重首推) |
| 基础工程：venv + 依赖锁定 | P1 | 未开工，等你确认方案 | §工程基础设施 |
| 整库冷备 + WAL checkpoint | P1 | 未开工 | [db-sqlite-refactor-plan.md](docs/db-sqlite-refactor-plan.md#p1--整库冷备--wal-checkpoint) |

> 建议一次只做一项，做完回填状态再挪下一个。数据分析 P0（口径统一 + 双 sort + 黄金测试）已于 09-08 完成。

---

## 工程基础设施 backlog（基础工程需求）

> 你提到"venv 之类的基础工程需求"，先列在这里，按优先级慢慢做。当前 `requirements.txt` 是裸跑无锁。

- [ ] **P1 venv + 一键安装**
  `get-a-job` 目前直接 `python3 -m gaj ...`，无虚拟环境、无可复现安装路径。
  目标：`setup.sh`（或等价）＝ `python3 -m venv .venv` + `.venv/bin/pip install -r requirements.txt`，README/AGENT 首装步骤收敛到一条命令。
- [ ] **P1 依赖版本锁定**
  `requirements.txt` 全无版本号（`fastapi>=0.100.0` 之类只是下限）。建议分 `requirements.txt`（运行）+ `requirements-dev.txt`（测试/开发），运行侧锁到可用版本区间。
- [ ] **P1 数据冷备 + WAL checkpoint**
  见 refactor plan §P1：`index.db` 启用了 WAL，直接拷 `index.db` 会漏 `.wal` 数据；需先 `PRAGMA wal_checkpoint(TRUNCATE)` 或 `backup()`。提供带时间戳的冷备脚本。
- [ ] **P2 Python 版本固定**
  运行时 Python 版本没写死（AGENT 里用的是 `python3`）。建议加 `.python-version` 或在 README 注明支持版本，避免环境漂移。
- [ ] **P2 首次安装文档联动**
  基础项落地后，把 README「安装」段、AGENT.md「前置条件」、`setup-chrome` 串成一条可照做的路径。
- [ ] **P2（候选）日志轮转**
  `logs/` 需核对是否无限增长；若是，加大小/天数轮转。此项先复核再决定。
- [ ] **P3（可选）打包成 pyproject**
  若后续要多机部署 / 提供稳定 CLI 分发，再做 packaging；个人工具阶段可长期缓。

---

## 数据库使用改进（P0–P3 backlog）

> 完整方案与改动点见 [db-sqlite-refactor-plan.md](docs/db-sqlite-refactor-plan.md)。这里只放清单。

| 优先级 | 项 | 一句话 |
|---|---|---|
| P0 | 公司分公式去重 | `apply_scope` 与 `refresh_company_stats` 各复制一份公式，抽公共函数 |
| P1 | 派生缓存 vs 历史快照 分离声明 | `index.db` 混装"可重建缓存"与"不可重建快照/纪元"，删除损失不一致 |
| P1 | 整库冷备 + WAL checkpoint | 避免拷贝丢 `.wal`，补日常备份 |
| P2 | 口径影子"写陷阱"防御 | scope 连接上写会落 temp 表丢失，防御后台线程误写 |
| P2 | 口径视图读文件（性能） | 每个带口径请求对 ~470 家公司读 `company.json`，应物化防抖 |
| P3 | FTS 只索引 JD 前 20000 字符 | 超长 JD 尾部不可搜，规模小先记录 |
| ✅ P2 | **口径多归属（重叠口径互吸岗位）（已做 09-17）** | 背景：`jobs.source_link` 单归属 + 采集「最后采集者赢」重归属，重叠口径（如无锡-20k 与前/后端，后者是前者职位面子集）有一方采集就把交集内在线岗位整体吸走（2026-09-15 实测翻走 26 岗，已临时归还修正）。**落地**：`scope_members` 多对多成员表（存量展开回填 1163 行）+ 采集侧 `upsert_scope_member` 只增登记 + `jobs.source_link` 冻结首归；`apply_scope` / `capture_snapshot` / reportbundle / web / CLI 圈定统一切成员表（快照圈定 = 本口径成员 ∩ 本口径 sighting 纪元，含空纪元容差）；`job.json` `provenance.scope_links` 持久化，重建不丢。57 tests passed（含黄金测试 `test_overlap_scope_no_steal`），真库冒烟 4 口径新旧计数全一致、副本库 capture 圈定 15/15 |
| P3 | 🆕 **快照下拉项带固化日期** | 下拉项现为「2026-09 · 2026-Q3 · 291 岗」，同月多份快照标签几乎相同、只能靠岗位数区分（09-15 实测苏州两份快照同标签）。改为含固化日期（如「09-08 · 291 岗」），修改点 `gaj/web/static/index.html` 快照 option 渲染 |

---

## 数据分析可信度与统计维度（P0–P2 backlog）

> 完整方案见 [data-analysis-improvement-plan.md](docs/data-analysis-improvement-plan.md)。
> 数据分析是项目最重要的能力，工程保障此前偏薄，这里按三层推进。

| 优先级 | 项 | 一句话 |
|---|---|---|
| ✅ P0 | 薪资口径统一（已做 09-08） | `avg_salary` 全引擎统一中位数，均值改名 `avg_salary_mean`，注册表说明已加 |
| ✅ P0 | 行业代表公司双 sort 修复（已做 09-08） | 合并为单次排序，"有公司分按分排前"生效 |
| ✅ P0 | 统计黄金测试（已做 09-08） | `tests/test_observatory_metrics.py` 已建，锚定均/中位语义防漂移 |
| P1 | 数据质量体检 CLI | `gaj qa data`：重复率/空值率/异常薪资/污染率/快照新鲜度 |
| P1 | 快照自动化 + 行情趋势 | 周期自动 capture + diff 持久化 + 趋势端点（岗位量/薪资 P50/技能热度环比） |
| P1 | 可信度透出 | 视图统一标注样本量 / 数据日期 / 门槛，低样本视图前端提示 |
| P2 | 求职者价值维度 | 薪资成长曲线 / 技能组合画像 / 岗位生命周期 / 期望校准闭环落地（calibrator 产历史）/ 公司通勤距离 —— **其中「薪资成长曲线 / 技能组合画像」已并入下方「个人发展路径」方案 L3，不在此重复立项** |
| P2 | 🆕 **`gaj strategy` 输出仍写「均薪」，实际是中位数** | 09-08 已把 `avg_salary` 全引擎统一为**中位数**（见上方 P0 行），**报告侧与网页端都已改对**（`reportbundle` v3.0「web 观察台技能榜均薪列同步改薪资中位」已落地，`web/static/views/*.html` 实测均写「薪资中位」，`reporter` 渲染层也一律「薪资中位」）。**唯一残留**：`gaj/strategy.py:114` 表头「均薪(万/年)」、`:136`「均薪 X 万」——这是 `gaj strategy` 命令行输出的 markdown，用户可见。→ 建议改「薪资中位」。**触发场景（2026-09-14）**：写苏锡对比稿时按字段名理解成均值，复算后才发现是中位，差一步就把中位数当均值写进笔记 |
| ✅ P2 | **`report-bundle` 支持按快照出报告（已做 09-15）** | 触发场景（2026-09-15）：写苏锡对比稿时人指定用**采集快照**口径（苏州 256 / 无锡 305），但 `gaj report-bundle` 只按 `source_link` 圈岗位（输出全量口径 358 / 391），没有「按 snapshot_id 出 bundle」的入口 → 报告/图卡数字与正文对不上。**落地**：`reportbundle` 新增 `snapshot_ref`（schema v3.1）——影子来源换成该快照的 `snapshot_members`（join 回 main.jobs 取全字段，聚合链路零改动）；`gaj report-bundle --snapshot <ref>` 支持 snapshot_id 或 period_month/quarter（后者需 --scope-link）；web `/api/report/bundle` 加 `snapshot` 参数；`meta.snapshot` 显式报数（含 missing_members）、指纹掺 snapshot_id；**指定 snapshot 时跳过顺手 capture_snapshot**（读快照不再生成快照）。`reporter` 侧同步加 `--snapshot` 透传（http/cli 双通路 + 台账记录），`--source bundle` 与 `--snapshot` 互斥。真实库冒烟：苏州快照 3106bc2015e04db2 出 bundle/report 均 256 岗/250 公司，快照总数不变 |
| P2 | 🆕 **跨纪元重采不使用岗位缓存（重复岗位强制重抓详情）** | 现状：dup 按 job_id 跳过详情页（`boss_scraper/crawler.py`），复现岗位的 JD/薪资/`last_seen`/`online` 永远停在首采版本（`touch_job_seen` 是死代码无调用方）——跨纪元快照对比时市场统计用的是旧薪资，内容失真。**已拍板（09-15）：跨纪元遇到旧纪元岗位 → 强制重抓详情**（migrate 合并路径已保留 first_seen，重抓顺带刷新 last_seen/online，D2 岗位生命周期分析因此有数据基础）；**同纪元内维持 dup 跳过**（daily 增量省请求）。实现提示：dup 判定需感知纪元（existing 集合改带 epoch 映射，旧纪元岗位不走跳过分支）；风控代价 = 跨纪元首采每口径多 ~300 次详情请求（约 2-3 小时，可分块跑） |

### 数据清洗（技能标签口径统一 · 2026-09-14 立）

> **归属原则**：清洗**只在上游 `gaj` 做**。下游（`gaj-reporter` 报告渲染、以及未来任何消费方）**不重复实现**——否则同一份数据两个口径会漂移。`reporter` 的渲染层只负责措辞与呈现，不做数据修正。
> **现状（2026-09-14 实测，别重复造）**：归一化**已经存在且工作正常**——`gaj/store/observatory.py` 的 `normalize_skill` / `iter_normalized_skills` 已实现：① 水印清洗（`kanzhun` / `boss` 子串，如「AI大kanzhun模型」→「ai大模型」）② 别名合并（`C#开发经验`→`c#`、`全栈项目经验`/`全栈无侧重`→`全栈`）③ 非技能停用词过滤（`可适应出差` 等）④ 岗位内去重。全库实测 **932 种原始标签 → 695 个规范键，水印残留 0**，`tests/test_reportbundle.py::test_skill_leaderboard_normalized_and_median` 已锁定。**下面三条是它还没覆盖的部分。**

| 优先级 | 项 | 一句话 |
|---|---|---|
| P1 | **斜杠复合标签未拆分** | 51 种 / 252 次出现（占全部标签出现次数的 **6.9%**）：`mysql/redis/mongodb`（64 次）、`spring/spring boot`（62 次）、`linux开发/部署`（26）、`django/flask`（10）、`vue/angular/react`、`javascript/typescript`、`mysql/sql server/oracle` 等被当成**一个**技能键 → 单个技能的 `demand_count` 被系统性低估（**Redis / MongoDB 可能因此进不了榜**）；`vue/angular/react`、`javascript/typescript` 这两种会**直接缩小正在做溢价对比的技能子集样本量**。建议在归一化规则里加一步「先按 `/`、`、`、`;` 拆分，再逐个走既有管线」 |
| P2 | **停用词表补漏** | 归一化后仍有背景要求混进技能键：`计算机/软件工程相关专业`（19 次）、`数学/统计相关专业`、`文档能力`、`团队协作能力`、`技术博客/开源项目有所贡献`、`有前端经验/技能`、`数据结构/算法能力`。其中多数**正是因为带斜杠才没命中停用词**，与上一条同源 |
| P2 | **岗位级覆盖率要透出** | **苏州 34.4% / 无锡 27.6% 的岗位归一化后完全没有技能标签**——归一化解决不了，属于采集端结构化字段缺失。建议：① 技能榜输出 `labeled_ratio`，让下游能标注「本榜基于 X% 的岗位」；② 评估能否从 JD 正文补抽技能（`jobs_fts` 已用 trigram 索引 JD，可行性待验） |

---

## 个人发展路径（能力规划 + 财务测算）· 待决策

> 完整方案见 [career-path-plan.md](docs/career-path-plan.md)。
> 方向：市场数据 + 个人技能画像 → 学习路径排序 → 学习投入的财务测算。**定位是辅助判断，不替代判断**（与「不做自动投递」同源）。
> 人 2026-09-10 提出，**明确「后续再推进」，当前未开工**。

| 优先级 | 项 | 一句话 | 状态 |
|---|---|---|---|
| P1 | **修画像解析层** | `parse_profile` 与 `profile.md` 字段对不上：实测薪资 / 城市 / 方向全为 `None`，技能被误命中「接受更换技术栈: 是」解析成 `['是']`；**静默失败不报错**，比崩溃危险 | 未开工，**无依赖，建议先做** |
| P1 | 画像补两个字段 | 「每周可投入学习小时」「可接受投入预算上限」——没有这两个算不了投入 | 零成本 |
| P1 | L1 路径排序 | 需求 × 溢价 × **邻近度**；当前只按需求量取 Top5，会推荐 Java / Vue 这类最卷或早就会的技能 | 待决策 |
| P2 | 技能溢价按职能分层 | 现 `salary_premium` 含混杂（`spring` −10% vs `spring cloud` +16.7%，后者仅 18 样本），须分层 + 出 p25/p50/p75 | **L2 前置，未落地不开工 L2** |
| P2 | L2 财务算账 | 输出投入 / 回本周期 / **概率门槛**；**不做收益预测** | 待决策 |
| P3 | L3 成长曲线 | 需跨期快照积累 | 远期 |

**核心判断**：技能溢价 ≠ 学习收益（相关非因果，溢价里混着赛道效应）。故财务部分用「反推回本门槛」替代「收益预测」——把不可验证的预测换成用户自己可判断的假设，且天然抗一键生成。

**待决策三项**：① 顺序（建议先解析层 + L1，L1 数据现成可快速验证付费意愿）② 财务口径确认采用「回本门槛」③ 是否现在补分层数据（schema 改动 + 重算）。

---

## 产品形态：桌面客户端 / 浏览器插件 · 待排期

> 人 2026-09-10 提出，两条均为 **P2**（一般优化，不影响正确性与数据），当前未开工。
> **这里是产品需求的正式落点**——内容线运营工作台 `gaj-reporter/ops/` 只管选题与发布，不登记产品需求（2026-09-10 纠正过一次归属）。

### 桌面客户端（Windows + macOS）

**目的**：把 `python3 -m gaj ...` 的流程做成开箱即用的图形界面，降低使用难度。现在要自己装 Python、手动连浏览器调试端口才能跑起来，非技术用户卡在第一步。

**开做前必须先定四件事**：
1. **选型**：打包现有 CLI，还是换个壳重写（前者省事，后者体验好）
2. **双平台构建与签名**：macOS 不签名公证会被系统直接拦；Windows 本机没环境，得靠自动化构建
3. **浏览器依赖怎么处理**：自带 / 首次启动引导 / 仍由用户装——**若浏览器插件落地，这一项可能被插件路径消化掉**（插件就跑在用户自己的浏览器里，不必再另起调试端口通道）
4. **CLI 是否继续维护**：客户端发版后命令行留不留

另需**预留本地接收通道**（供浏览器插件上报的岗位入库）。

### 浏览器插件

**想法**：用户在站内正常浏览岗位时，主动把当前看到的岗位上报给本地客户端，作为采集的补充来源——少跑爬虫，也更贴平台规则。

**三条判断**：
1. **合规逻辑成立，但硬前置是核平台协议**——插件只搬用户当前页面上已公开的岗位信息、由用户主动触发，不做后台批量请求、不模拟登录、不绕风控，行为发生在真人正常浏览中。但必须先核平台用户协议里对第三方插件 / 自动化工具的条款，不能凭感觉；另加权限最小化（只申请目标站点域名，不读其它页面）。
2. **样本偏差是真风险**——用户手动刷到的是**他感兴趣的岗位，不是随机抽样**。有偏样本混进统计会污染报告口径（报告线的核心资产就是样本无偏），故定位只能是**补充层**，上报数据**必须打来源标记**，分析时可单列或剔除；基准样本仍靠无人值守采集。
3. **插件比客户端轻，可先行**——无签名公证、无双平台构建，用「插件 + 现有命令行」就能先闭环验证。但数据**必须进同一套清洗 / 去重 / 口径管线**（去重指纹、冷却窗口、一个筛选链接一个口径），不能另起一套；上报字段需与现有抓取字段对齐（列表页卡片字段可能少于详情页）。

**两者关系**：客户端是入口与本地库，插件是数据入口——接收接口一起设计，别各做各的。

---

## 长期方向（旧 TODO.md 归并，附复核标注）

- [ ] **行业政策分析**（未做）
  行业观察/下钻已上线，但"结合行业政策做分析"这一后续能力尚未实现。——对应旧 TODO 第 2 条后半。
- [ ] **市场观察数据 → 优化规则 / 画像预期**（模块在，未落历史）
  `gaj/ai/calibrator.py` 已存在，但 `data/calibration/` 为空——矫正机制已就位、尚未产生落地历史。→ 旧 TODO 第 3 条。
- [ ] **公司 - 本人通勤距离**（未实现）
  `profile.md` 只有"单程通勤上限"评分规则 + 居住地坐标占位（注释"后续用于计算通勤时间"）；公司-本地的距离/通勤时长计算尚未实现。→ 旧 TODO 第 4 条。

---

## 已完成归档（旧条目，莫再当 TODO）

- ✅ **模块 / 仓库拆分**：`get-a-job`（数据+采集+评分+图鉴 Web）与 `gaj-reporter`（报告/内容线）已分仓库，`gaj/store` 内有 `repo / index / observatory / reportbundle` 分层。——旧 TODO 第 1 条。
- ✅ **行业观察 + 下钻查询**：`observatory_industry_list` + Web `/industry/{name}` 下钻已上线。——旧 TODO 第 2 条前半。

---

## 更新日志

- 2026-09-19（09:20）：**采集防风控事故：一夜连采 534 岗被 BOSS 封禁 + 落地「24h 滚动配额」**。经过：00:16–06:30 用无人值守分块链连采上海 + 杭州两个口径（同一 `crawl.lock` 串行、`caffeinate` 防休眠），**6.3 小时不间断抓了 534 个岗位详情**（约 85 个/小时、单岗位 ~42s）；06:33:43 杭州第 17 页列表 API 返回 `code=32`「您的账户存在异常行为，已暂时被禁止使用」，3 次重试全挂，紧随的续采块在第 1 页又被拒 → 判定**账号级临时封禁，持续数日**（09-19 08:56 复探仍封，已停掉自动续采避免探活拖长封禁）。封禁前战果：**上海 298 岗**（第 20 页 `hasMore=False`，BOSS resCount=450 但列表只暴露 20 页，已到尽头；薪资中位 36.0 万、P25 27/P75 49、外包驻场 13）、**杭州 236 岗**（薪资中位 29.9 万、P25 24/P75 39、外包 15），杭州断在第 17 页。**落地**：①`CrawlConfig.max_jobs_per_24h=350`（0=不限）24h 滚动窗口配额，达到即 `early_stop_reason=budget_24h` 且**连列表 API 都不发**；计数口径 = 索引里 `first_seen` 落在窗口内的岗位数（抓一个即增量入索引，`kill -9` 不丢计数，无需新增计数文件、无数据结构变更）；页首 + 页内逐岗位双重检查，停止时记录续翻锚点。②`status` 暴露 `budget_24h{cap,used,remaining}`，`crawl` 返回体与 `last_runs` 归档同带，用于区分「正常限流」与「故障」；AGENT.md 新增「24h 采集配额」章节、退出机制补第 4 种停止原因、crawl help 同步。**验证**：57 passed；真机跑配额用尽场景 `pages=0 / jobs_scraped=0 / budget_24h`，耗时 15.5s 未发列表请求。**自我纠错**：初版 `job_budget or None` 会把 `remaining=0` 转成「不限」使闸门完全失效，是端到端真机跑才暴露的，已改为 `None`=不限、`0`=一个都不采。**遗留**：杭州断点锚点 17 已存好（`data/crawl_state.json`，用 `--start-page` 修回），解封后重跑 `crawl` 自动续采（已挂入当前优先队列）；采集频率是否需进一步收紧（如单城限时、加冷却窗口），待配额实跑观察后再定。事故边界：本次封禁为账号级，非单口径/单城市问题，多口径串行连采同样计入总量。
- 2026-09-19（09:05）：**修复「续翻断点被浅页失败冲掉」**。起因：排查上述封禁时发现杭州断点从 17 被回写成 1（日志 `锚点无进展 (17 -> 1)`）——采集从第 1 页重扫时第 1 页即被反爬拒绝，`crawler.py` 的失败分支直接 `last_dup_page = page`，把上次存下的深断点覆盖成浅页码，下次只能在浅页附近打转、**永远翻不到断点之后的内容**（属静默数据缺失，比采集中断更隐蔽）。落地：新增 `JobCrawler._record_resume_anchor`，把「翻页上限 / 获取失败 / covered / 中断」四处写入点统一收口，**锚点只进不退**（下限取本次载入的 `resume_page`，手动 `--start-page` 时以调用方为准），仅 `hasMore=False` 走显式重置 0。adapter 持久化注释与 CLI help 同步。验证：6 个场景逐一校验（反爬拒浅页保持 17、续翻更深前进 19、无断点 covered 记 3、翻页上限记 21、手动跳页以调用方为准、翻到底仍 0）+ 57 passed。
- 2026-09-17（23:55）：**升级体验加固（起因：陆续有外部用户开始使用，本次口径多归属含 BREAKING 语义）**。①`index._migrate` 检测到老库首次升级 scope_members 时，迁移前自动做一次带时间戳的全库冷备（新增 `_cold_backup`，sqlite backup API 落 `data/backups/index-<ts>-pre-scope-members.db`，失败只告警不阻塞迁移），迁移结果在日志显式报数——所有入口（web/CLI/agent/采集）都经 `connect()`，升级对使用者透明；已用模拟老库验证：首连冷备+回填+二连不重复冷备。②面向用户的 CHANGELOG.md 新增 v0.10.0 条目（升级说明 + BREAKING 标注：scope-link assign 语义、重归属改只增登记）。③AGENT.md「注意事项」新增**数据兼容红线**：存量数据不兼容改动必须同时满足自动迁移（幂等+日志报数）、迁移前自动冷备、CHANGELOG 升级说明三条，禁止要求用户手工迁移/重建/丢数据。
- 2026-09-17（23:30）：**完成「口径多归属」（P2，09-15 事故当日立项，两日落地）**。核心改动：新增 `scope_members(source_link, job_id, first_seen_at, last_epoch_id)` 多对多成员表，迁移把存量 `jobs.source_link` 一行展开为一条成员行（真库回填 1163 行）；归属写入从「改写 source_link」改为 `upsert_scope_member` **只增登记**（列表 sighting / 详情入库 / 收尾兜底 / 手工 assign 统一走它，存在则仅推进本口径 `last_epoch_id`），`jobs.source_link` 冻结为**首次归属**不再被翻走；文件侧 `job.json` 持久化 `provenance.scope_links`（append-only）与 `scope_member_epochs`，index.db 可从文件重建不丢成员；圈定消费方统一切成员表——`apply_scope` 影子、`capture_snapshot`、reportbundle scope 模式、web 口径统计、`scope-link list`；快照圈定语义改为**本口径成员 ∩ 本口径 sighting 纪元**（含 ''/NULL 空纪元容差），跨口径采集不再互相稀释快照；`sync_scope_members` 幂等兜底直改 DB 的存量数据。BREAKING：`touch_job_source_link` 改写语义废除，`reassign_source_links` 从全量重归属改只增登记。测试 57 passed（新增黄金测试 `test_overlap_scope_no_steal`：口径 B 采集后口径 A 的岗位数与快照指标不变）。真库冒烟（先冷备、后只读核验、再副本库冒烟）：4 口径 old/new 计数全一致（前/后端 391、无锡-20k 399、苏州 358、数据分析 15），孤儿成员 0，成员总数 1163 = 有 source_link 岗位数 1163，`apply_scope` 影子 399 = old_n；副本库 `capture_snapshot`（数据分析口径）：返回 job_count 15 = 手写圈定 SQL 15，`snapshot_members` 15 行，快照 +1（a463c8de92a14d31，15 岗/15 公司），capture 前后成员行数不变（本口径 15→15、全库 1163→1163），旧纪元正常闭合、新纪元开启。冷备：`data/backups/index-20260917-pre-multimembership-smoke.db`。
- 2026-09-15（20:50）：**完成「report-bundle 支持按快照出报告」（P2，当日立项当日落地）**。`reportbundle` schema 升 v3.1（只增字段）：`build_report_bundle` 新增 `snapshot_ref`，影子来源从「scope 全量」换成快照 `snapshot_members` 成员集（join 回 main.jobs 取全字段，聚合链路零改动）；CLI `--snapshot`、web `snapshot` 参数；`meta.snapshot` 报数 + 指纹掺 snapshot_id + 指定 snapshot 跳过 capture_snapshot。reporter（gaj-reporter）同步加 `--snapshot` 透传与台账字段。双仓测试全绿（53 + 46），真实库冒烟：苏州快照出 bundle/report 均 256 岗，无新快照产生。
- 2026-09-15（20:20）：**立项「`report-bundle` 支持按快照出报告」（P2）**。起因：写苏锡对比稿时人指定用**采集快照**口径（苏州 256 / 无锡 305），但 `gaj report-bundle` 只按 `source_link` 圈岗位、输出的是**全量口径**（358 / 391，含该口径历史全部岗位），**没有按 `snapshot_id` 出 bundle 的入口** → 报告渲染出的报告/图卡样本量与中位数跟正文对不上。已立入「数据分析可信度与统计维度」P2，未开工。落地要点：`reportbundle` 的聚合链路已按 `temp.jobs` 影子工作，理论上只需把影子来源从「scope 全量」换成「`snapshot_members`」；⚠️ 需跳过 `reportbundle.py:880` 原有的「出报告顺手 capture_snapshot」，否则会读快照又生成快照。
- 2026-09-15（20:08）：**立项「快照下拉项带固化日期」（P3）**。起因：人在 web 工作台看到快照下拉「实时数据 + 2 份快照」共 3 项产生疑惑（苏州只采了 2 次哪来 3 个选项），排查确认「实时数据」是固定内置项而非快照，2 份快照与 2 次采集一一对应，数据无异常；但两份同月快照标签几乎相同（均为「2026-09 · 2026-Q3」）只能靠岗位数区分，故立此改进项。顺带厘清并记录：「实时」口径影子（`apply_scope`）只按 source_link 过滤、**不筛纪元**，纪元过滤仅存在于快照固化影子——实时视图语义是「口径全量（含历史纪元）」。**已拍板（09-15）：保持现状**，实时 = 口径全量，纪元只服务快照隔离。
- 2026-09-15（19:55）：**拍板「跨纪元重采不使用岗位缓存」**。起因：排查确认 dup 跳过机制使复现岗位的 JD/薪资/`last_seen`/`online` 永远停在首采版本（`touch_job_seen` 死代码无调用方），跨纪元快照对比存在内容失真。决策：**跨纪元遇到旧纪元岗位强制重抓详情；同纪元内维持 dup 跳过**。重抓顺带刷新 last_seen/online，给 D2 岗位生命周期分析补上数据基础。已立入「数据分析可信度与统计维度」P2，未开工。
- 2026-09-15（14:35）：**重叠口径归属漂移事故 + 临时修正 + 立项「口径多归属」**。新纪元重采时误把「无锡-20k 多岗位」观察口径一并重采（本意只采前/后端 + 苏州），20k 采集依「最后采集者赢」规则把 **26 个 ≥20k 岗位**从前/后端口径翻走到 20k 口径（前/后端高薪段被稀释，P90 30→28.2）。已临时修正：26 岗归还前/后端（当天经 20k 结果页验证在招，归还安全）+ 当日 279 岗并回活跃纪元 + 重导快照（**305 岗/293 公司**，P90 回到 30.4）；修正前的 279 岗快照（24a2d48d）当时留档，同日经确认已随垃圾数据一并清理（同批清理：苏州 4 条 0 岗空快照 + 'xx' 测试纪元；清理前已做 index.db 冷备至 data/backups/）。事故边界：单归属漂移只发生在两口径筛选条件的交集内。后续迭代见「数据库使用改进」P2 新行——**允许岗位在多个口径里被统计**（多对多成员，重归属只增不减）。
- 2026-09-14（22:30）：**新增「数据清洗（技能标签口径统一）」区**，人拍板「清洗只在上游做，`reporter` 里不重复实现」。立此区前先实测了一遍现状，**纠正一个误判**：技能标签的归一化**不是没做，是做过了**——`gaj/store/observatory.py` 的 `normalize_skill` 已覆盖水印清洗 / 别名合并 / 停用词过滤 / 岗位内去重，全库 932 种原始标签归一到 695 个规范键、**水印残留 0**、黄金测试已锁定。真正还没覆盖的是三条：**① 斜杠复合标签未拆分**（`mysql/redis/mongodb` 等 51 种 / 252 次，占 6.9%，会让 Redis / MongoDB 这类技能的需求量被低估、甚至进不了榜）**② 停用词表补漏**（`计算机/软件工程相关专业` 等，多数因带斜杠而漏网）**③ 岗位级覆盖率要透出**（苏州 34.4% / 无锡 27.6% 的岗位根本没有技能标签）。另把「`gaj strategy` 输出仍写均薪」收窄为唯一残留点（报告侧与网页端已改对）。
- 2026-09-10（20:31）：**产品需求归属纠正**——「桌面客户端 / 浏览器插件」不再寄存在 `gaj-reporter/ops/` 需求池（ops 是内容线运营工作台），正式落为本文「产品形态：桌面客户端 / 浏览器插件 · 待排期」区，均 P2；原 R001 / R003 编号作废。两条关键判断保留：插件采集的**样本偏差**风险（手动刷到的是非随机样本，只能作补充层 + 打来源标记）、插件**可先于客户端独立落地**（无需签名公证与双平台构建）。
- 2026-09-10：新增「个人发展路径（能力规划 + 财务测算）」待决策区，完整方案见 [docs/career-path-plan.md](docs/career-path-plan.md)。**顺带实测发现一个既存缺陷**：`gaj/strategy.py::parse_profile` 与 `data/profile.md` 字段不匹配，解析结果为 `salary=None / city=None / skills=['是']`，属静默失败——已记为方案 P1 首项。
- 2026-09-08：完成数据分析 P0（`avg_salary` 全引擎统一中位 + `avg_salary_mean` 均值保留、行业代表公司双 sort 修复、黄金测试锚定口径）。明细见本次 git commit。
- 2026-09-07：新增「数据分析可信度与统计维度」backlog（P0 口径一致挂入当前优先队列首位），完整方案见 [data-analysis-improvement-plan.md](docs/data-analysis-improvement-plan.md)。
- 2026-09-06：建档，取代粗糙 `TODO.md`；归并旧条目并标注实际完成状态；挂入基础工程 backlog 与 SQLite 改进 backlog。
