# 坑位图鉴 — 版本日志

版本号遵循语义化版本（SemVer）：`MAJOR.MINOR.PATCH`。

- `MAJOR`：破坏性架构变更

- `MINOR`：新增能力，向后兼容

- `PATCH`：Bug 修复 / 体验优化

***

## \[v0.8.0] — 猎头/代招识别（H-11）与水印清洗补强

> 2026-09-16。列表 API 里早就带着招聘者、金牌猎头、匿名、代招这些结构化字段，
> 但迁移时被丢掉、打分侧也没人消费，等于白采。本次把这条管线接通并落成硬规则；
> 同时补上 JD 文本里漏网的拉丁字母水印碎片。

### 新增

- **H-11 猎头/代招帖**: 只用列表 API 的结构化字段判定，闸门复用画像的「接受外包岗位」开关。
  招聘者头衔含「猎头」或平台标记金牌猎头 → conf=1.0 直接淘汰；代招标志
  （`proxyJob`/`proxyType`）→ 0.6；匿名雇主（`anonymous=1` 或公司名「某…公司」）→ 0.5 标 REVIEW 交 AI。

- **列表 API 字段回填**: `migrate` 读采集目录 `_debug/joblist_page_*.json`，按 `encryptJobId`
  建索引，回填招聘者（`job.boss`）、匿名、代招标志；城市也改为「列表 API > 详情页地址 > 人工假定」。
  迁移报告新增「回填列表API字段」计数。此前 `job.boss` 恒为空（实测 231 条存量记录中非空 0 条）。

- **`job.boss` 新增字段**: `anonymous` / `proxy_job` / `proxy_type`（来自列表项）。

- **口径扩池（`gaj scope-urls`）**: 单口径池子会见顶（`resCount` 虚高，实测单口径实得 ≈30 条），
  用「关键词 × 筛选条件」批量生成口径 URL —— 主词跑全量组合（38 条 = 1 基线 + 37 编码），
  其余同族词跑精选组合（17 条）；`--out` 落文件、`--label` 给建议口径名（配合 `scope-link rename`）。
  新增 `gaj/core/scope_urls.py`（码表 + URL 构造 + 反解析/命名）。
  城市码**只收录已验证的 6 城**，其余走 `--city-code` 裸码或 `references/boss_city_codes.json`。

- **`gaj export-filter-codes`**: 在已登录页面导出筛选下拉的 `ka` 编码，与内置码表 diff 后写入
  `references/boss_filter_codes.json`（生成器优先读它）—— 编码漂移时不用人肉比对。
  新增 `gaj/scraper/filter_codes.py`。

### 修复

- **水印漏网（拉丁字母碎片）**: 原清洗只覆盖「来自BOSS直聘」家族，`kanzhun`（看准网拼音）与
  夹在汉字之间的 `boss` 会留在正文里，且 `looks_polluted` 用同一份词表 → 质量校验把这批 JD 判为干净。
  实测 231 条历史 JD 中 9 条命中，已按新规则重洗并刷新索引（复核残留 0）。
  新增规则：`kanzhun` 一律剥离；`boss`/`zhipin` 只在**两侧都是汉字**时剥离（保住正文里的合法英文串）。

### 说明

- 规则版本 4.0 → 4.1；存量岗位未重打分（历史数据没有招聘者字段，H-11 现在不会命中，
  下次采集后自动生效）。

***

## \[v0.7.0] — 采集纪元与快照（同口径多期对比）

> 2026-09-04。新增顶层能力: 同个口径每隔一两个月重新采集时，按「采集纪元」隔离数据。
> 一次性导出即固化历史快照，支持跨月/跨季度 diff 对比，且单次采集中断可原地续跑、不重启新纪元。

### 新增

- **采集纪元（collection\_epoch）**: `jobs` 新增 `collection_epoch` 列 + 各口径维护「当前活跃纪元」
  （`collection_epochs` 表）。新采集写入活跃纪元，与旧数据物理隔离。

- **异常恢复**: 单次采集若中断，续跑沿用当前纪元、不重启新一轮（避免旧月份数据污染新快照）。

- **快照（observatory\_snapshots + snapshot\_members）**: 导出 report 时固化当前活跃纪元的可见岗位
  与市场六维指标，形成不可变快照；快照内岗位/公司成员明细落库，支持历史回溯与同口径 diff。

- **Web 快照切换**: 侧边栏「数据口径」下方新增「快照」选择器，市场观察的薪资/热力/雷达/技能
  四个子视图可按快照回看历史（需先选口径）。

- **CLI**: 报告生成器支持从指定快照导出（`gaj-reporter`）。

### 变更

- 快照后新采数据与旧纪元隔离存储，杜绝「11 月快照混入 9 月岗位」的错误影响；旧纪元数据仍可调出比对。

### 修复 / 体验

- **分位条（岗位/公司详情）**: 重构 P10/P25/P50/P75/P90 标签布局为上下两排交替 + 同排防重叠，
  刻度线恒定落在分位点；解决标签挤作一团或飘出容器的问题（此前静态锚定方案在部分分布下失效）。

- 职位列表筛选框两列排版优化；四大视图副标题居中；移除 landing 重复截图区块。

### 测试 / 文档

- 快照捕获/回溯/纪元隔离相关用例；design docs 见 `.trae/specs/obs-snapshot-diff/spec.md`；
  landing（docs/index.html）更新职位详情截图与首屏文案。

***

## \[v0.6.0] — 来源口径体系（source\_link 全链路）

> 2026-08-31。新增顶层能力: 一个 BOSS 筛选链接 = 一个数据口径。
> 入库带链接 → 按口径隔离出报告 → Web 全局口径切换 → 报告标题用口径命名。
> 数据契约升级 **bundle schema 2.2 → 3.0**（消费方 gaj-reporter 已同步）。

### 新增

- `jobs.source_link` 字段 + `source_links` 口径注册表（link/label/created\_at）:
  采集迁移链路（adapter → migrate → job.json → 索引）全链路透传来源链接。

- `build_report_bundle(scope_link=, include_ignored=)`: 口径隔离出报告
  （temp 表影子实现, 聚合代码零改动）; **默认包含已忽略岗位**（个人忽略不影响
  市场统计, `meta.scope.ignored_included` 显式报数）。

- Web「口径管理」页（侧边栏「口径管理」）: 口径列表/岗位数/重命名
  （命名用于报告标题）/设为当前口径; 长链接完整多行显示。

- **全局口径筛选**: 侧边栏顶部「数据口径」下拉, 职位/市场观察/公司图鉴/统计
  等全部只读视图按当前口径过滤（contextvar 请求级隔离; 写路径永不套用）。

- CLI: `report-bundle --scope-link --exclude-ignored`;
  `scope-link list / rename / assign`（历史数据手工补归属）。

### 变更

- **schema 3.0（破坏性）**: 技能榜与公司双榜唯一实现下沉 observatory
  （归一化 + 中位数 + 市场中位溢价, 与行业溢价同口径）;
  `company_boards` 删除 `hiring_lite/salary_lite` —— 脱敏代号化移交
  gaj-reporter 渲染层独立完成（lite 真名零泄漏实测）。

- 观察台技能榜列「均薪」→「薪资中位」。

- 历史数据归集: 全部 364 条（可见 269）归入「无锡-全栈工程师-主口径」,
  未分口径归零; 口径指纹掺入链接哈希（不同口径指纹必然不同）。

- 去重保证: `job_id` 主键 + `source_link` 单值 —— 同岗位被多链接重采
  只保留一行并归最新口径, 合并展示不重复（测试锁定）。

- **列表级口径重归属**: 新口径采集后, 凡出现在本次筛选列表中的历史岗位
  （含被跳过详情的重复岗位）自动挪入新口径 —— 修复「已采集过, 跳过」
  导致历史岗位不随新口径更新的问题。

### 测试

- 24 passed, 新增: 口径隔离/命名/影子拆除恢复/重采去重/
  技能榜与双榜同源断言（bundle == observatory 完全相等）。

- 手册: `docs/scope-link-guide.md`。

***

## \[v0.5.2] — 报告契约 schema 2.2（榜单候选池扩容）

> 2026-08-30。

### 变更

- 榜单候选池扩容: 行业 8→12 / 公司双榜 10→30 / 技能榜 15→30
  （`build_report_bundle` 可传参; 入口默认同步）。

- 报告生成器按样本量自适应取 Top N（TOPN\_LADDER 阶梯, 见 gaj-reporter）,
  契约本身只承诺「足够大的候选池」。

***

## \[v0.5.1] — 报告契约 schema 2.1（读者价值审计）

> 2026-08-30。审计结论驱动：报告的两类付费读者（应届生/跳槽者）逐维度审计后，
> 补齐三个高价值维度并统一报告口径；审计文档见 gaj-reporter 仓库 `docs/`。

### 新增

- `market.functions`：岗位职能分桶（标题关键词规则，规则随包公示）× P25/P50/P75 × 未标注经验占比 × 技能 Top3。

- `market.career_entry`：应届生入门口径 —— JD 未标注经验门槛岗位聚合 + 剔高级岗子集 + 学历/城市/行业/公司在招分布。

- `market.local_pricing`：主导城市口径 vs 全样本对照，未标注城市岗位如实报数。

- 完整版双榜条目新增 `city`（岗位标注城市众数；lite 版不带，防反推）。

- 测试：+6 项契约测试（15 passed）。

### 变更

- 技能标签归一化：别名合并（C#开发经验→c#）+ 后缀剥离 + 停用词（可适应出差等非技能词）+ 岗位内去重；技能种类 463 → 约 440。

- 反爬水印清洗：抓取源在技能串内注入的 `kanzhun`/`boss` 标记（实测污染 39 岗）在归一化时剔除。

- 报告口径统一（bundle 层）：技能榜改中位数 + 市场中位溢价基准；信号公司/焦点代表公司/区县 `avg_salary` 键名不变、语义覆写为中位数。gaj web UI 聚合语义（均值）不变，报告侧在 reportbundle 重算。

***

## \[v0.5.0] — UI/UX 系统性迭代与行业观察

> 依据 [市场洞察迭代计划](../.trae/documents/market-insights-iteration-plan.md) 与
> [行业观察功能设计](../.trae/documents/industry-observation/2026-08-27-行业观察功能设计.md) 落地。
> 设计文档见 `.trae/documents/ui-redesign/`。

### 🎨 UI/UX 系统性迭代（信息架构重构，保持原视觉基调）

- **侧边分组导航**：工作台（职位列表/公司图鉴）· 市场（市场观察/行业观察）· 配置（规则配置/主简历），所有视图 1 次点击可达

- **顶栏瘦身**：13 元素 → 品牌 + 统计胶囊 + 批量 + 规则打分；配置/简历从 toggle 按钮改为导航项

- **统计胶囊直达筛选**：职位/规则分/AI分/未打分/收藏/已忽略 点击即切视图并预置筛选

- **公共组件层**（`styles/components.css`）：焦点环 / 统一 Tab / 工具栏 / 统计卡 / 空态；三套 Tab 合并为一种

- **公司抽屉全局化**：职位列表、公司图鉴、市场观察三处统一复用（就地打开，不跳视图）

- **TODO 落位**：职位详情「查看公司图鉴」入口（#1）、公司抽屉「市场数据」区块（#2）、职位详情「市场参考」区块（#4，复用观察台接口 + 会话缓存）

- **细节**：移除 🤖/⏳/⚠ emoji 图标、favicon SVG 化、次要文字对比度提升、`--gaj-focus-ring` 令牌

### 📈 新增：行业观察（TODO#3 · 方案 B）

- **后端**：`/api/observatory/industry`（行业总览：岗位/公司/薪资中位/红旗率/技能 Top3）+ `/api/observatory/industry/{name}`（单行业详情：薪资分位 P25/P50/P75、经验-薪资、技能 Top10、红旗信号、代表公司 Top8）；样本不足自动降级，纯 SQLite 聚合无新依赖

- **前端**：侧边导航「行业观察」启用；行业卡片墙（搜索/按岗位数·薪资·红旗率排序）→ 行业详情页；代表公司 → 全局公司抽屉、岗位预览 → 职位详情链路打通

- **政策分析挂载位**：行业详情页预留「行业政策分析 · 规划中」区块（远期接入外部政策文本 + AI 解读）

- **联动**：市场观察薪资/雷达区块新增「行业观察」入口

- **行业详情补齐市场维度**：学历-薪资（行业口径）、区域分布 Top10（岗位聚集区县，点击下钻职位列表并带行业+区域筛选 + 可清除的筛选提示条）

### 📊 市场定位与象限迁移

- **公司象限图迁移**：从公司图鉴移出（图鉴保留卡片墙/对比），改为共享组件 `core/quadrant.js`，落地两处——市场观察新增第 5 个 Tab「公司象限」（全市场公司均薪-分数分布，点击气泡开公司抽屉）、行业观察详情页新增「公司象限」区块（该行业公司分布）

- **岗位详情「市场参考」增强**：新增行业薪资分位条（P25/P50/P75 区间 + 本岗位置点 + 高/低位判定）与经验对标（岗位经验段匹配行业经验-薪资中位）

- **公司抽屉「市场数据」增强**：新增行业分位条（公司均薪定位）与规模对标（公司在招 vs 行业平均公司规模）

- **行业详情数据口径统一**：代表公司 Top8 改用与公司象限同源的数据（同一公司统计口径，有分按公司分、无分按在招数），两区块分数/集合完全一致

- **公司象限视觉统一**：市场观察「公司象限」改为区域热力同款结构（obs-chart 图表容器 + 等级图例 + 「公司象限榜 Top 10」配套表，行点击开公司抽屉）

- **行业下钻打通**：市场观察所有行业触点可进入行业观察详情——薪资/雷达行业柱图点击、技能榜行业标签、热点区域表行业按钮、下钻岗位详情行业字段

- **薪资分位条重设计**：细轨道（10px）+ 色块贴合圆角（overflow 裁剪）+ 刻度线与 P25/P50/P75 标签下移留白 + 当前值改为轨道上方菱形标记（高位绿/低位黄），岗位市场参考与公司抽屉市场数据两处统一

### 🧭 体验优化

- **hash 路由**：视图切换同步浏览器 URL（`#/jobs` `#/guide` `#/observatory` `#/industry` `#/config` `#/resume`），浏览器前进/后退可返回上一模块（视图状态保留）；带 hash 刷新直达对应视图

- **命名统一**：「市场观察台」→「市场观察」，导航分组「市场洞察」→「市场」（市场观察 = 市场维度 / 行业观察 = 行业维度 对称体系）

- **行业详情区域分布样式补齐**：区域行卡片 + 机会密度条

- **日间模式**：顶栏一键切换日/夜（全套 --gaj-\* 令牌浅色值，无需逐视图适配）；localStorage 持久化，首次访问跟随系统偏好；sticky 栏等半透明表面随主题切换

### 🧹 全面质检（代码/UI/UX 巡检）

- **修复行业详情接口含斜杠行业名 404**（如「汽车研发/制造」点击无响应）：路由改用 path 转换器

- **日间模式细节**：象限图 C 级气泡填充改用表面色（原白色 4% 透明在浅底不可见）；行业薪资条/徽章/图例确认全部走变量

- **窄屏对称性**：900px 断点下 sticky 摘要栏负边距与内容区对齐

- **死代码清理**：store.toggleView / store.marketForIndustry / store.openCompany / openDrill 的 industry 类型分支（均无调用方）

- **巡检结论**：6 视图 × 暗/日两主题 × 1440/800 两宽度全链路自动化巡检 0 控制台错误、0 横向溢出

### 🎨 口径 UI 融入全局体系

- **侧边栏口径筛选重构**：去除内联样式与 2px 粗边框，改为与分组同节奏的「数据口径」标签 + 主题化下拉（选项精简为命名、岗位数入 title），窄屏（≤900px）自动隐藏防挤爆

- **口径管理导航项统一**：移除无样式的双行结构，回归标准单行 nav-label

- **口径管理页重做**：容器复用 config-body（滚动 + 居中），表格对齐 ind-table 风格（行 hover/激活高亮 primary-muted），删除自带 .btn/.input 重复组件改用全局控件，内联样式全部收编进 scope.css

- **市场观察口径徽标**：tabs 右侧显示当前口径（切口径后数据来源一目了然）

- 修复口径管理页内容超高被裁剪（补滚动容器）

### 🐛 修复

- 规则配置页样式误删恢复（基线合并时清理脚本范围过宽）

- 配置/主简历页 Tab 与内容间距、底部留白

- 顶栏统计胶囊 hover 上浮被截断（改 inset 边框反馈）

- 市场观察公司抽屉空白（静态资源版本号未随改动升级，浏览器缓存新旧混合）

- 行业详情页异步渲染后 Lucide 图标不刷新（图标刷新依赖补全 items/detail）

***

## \[v0.4.0] — 前后端功能域分治重构（组件岛迁移完成）

依据 [ADR-002](../.trae/documents/adr-002-module-split.md) 完成 ADR-001 预留的「逐视图迁出」迭代，
**API 与交互行为零变化**，代码按功能域一域一文件。模块地图见 [gaj/web/README.md](../gaj/web/README.md)。

### 🏗️ 前端：删除 1023 行单体 app.js，五岛齐备

- **视图三件套**：一视图 = `static/views/<name>.js`（逻辑）+
  `views/<name>.html`（模板片段）+ `styles/<name>.css`（样式），
  改哪个视图只读这三个文件，不碰 index.html

- **index.html 1946 → \~200 行**：只留共享 UI（Toast/Header/日志面板）、
  各视图岛挂载点（`data-tpl`）与模板 loader

- **模板片段加载**：loader 先 fetch 各视图模板注入挂载点，全部完成后
  才动态加载 Alpine（本地 alpine 3.15.12 无 deferLoadingAlpine 钩子，
  用「先注入后加载」保证时序）；fetch 失败显示 #tpl-error 兜底块

- **视图路由单一真相源**：`$store.core.view` 取代 showGuide/showConfig 等 4 个布尔 flag

- **共享层扩容**（`core/store.js`）：taskRunning/scoreAll/跨视图跳转
  openJob/openCompany/bootstrap；跨视图通信统一 window 事件协议
  （`gaj:open-job` / `gaj:open-company` / `gaj:refresh` / `gaj:toggle-select`）

- **公司详情抽屉移入公司图鉴岛**；观察台「在主视图打开」现在会真正切换视图

- **CSS 按视图抽离**：新增 `styles/{jobs,guide,config,resume}.css`；
  `style.css` 1980 → \~300 行共享外壳，并删除与 `base.css` 重复的令牌段

### 🏗️ 后端：app.py 拆为纯组装层

- 路由全量按域拆入 `routes/`：新增 `jobs.py` / `scoring.py` / `profile.py` /
  `resume.py` / `system.py`（SSE 日志桥随日志流进 system）

- `routes/__init__.py` 提供 `ALL_ROUTERS` 聚合表，新增路由零改 app.py

- app.py 725 → \~90 行：只保留 app 工厂/生命周期/静态资源/启动入口

### 📊 市场定位与象限迁移

- **公司象限图迁移**：从公司图鉴移出（图鉴保留卡片墙/对比），改为共享组件 `core/quadrant.js`，落地两处——市场观察新增第 5 个 Tab「公司象限」（全市场公司均薪-分数分布，点击气泡开公司抽屉）、行业观察详情页新增「公司象限」区块（该行业公司分布）

- **岗位详情「市场参考」增强**：新增行业薪资分位条（P25/P50/P75 区间 + 本岗位置点 + 高/低位判定）与经验对标（岗位经验段匹配行业经验-薪资中位）

- **公司抽屉「市场数据」增强**：新增行业分位条（公司均薪定位）与规模对标（公司在招 vs 行业平均公司规模）

- **行业详情数据口径统一**：代表公司 Top8 改用与公司象限同源的数据（同一公司统计口径，有分按公司分、无分按在招数），两区块分数/集合完全一致

- **公司象限视觉统一**：市场观察「公司象限」改为区域热力同款结构（obs-chart 图表容器 + 等级图例 + 「公司象限榜 Top 10」配套表，行点击开公司抽屉）

- **行业下钻打通**：市场观察所有行业触点可进入行业观察详情——薪资/雷达行业柱图点击、技能榜行业标签、热点区域表行业按钮、下钻岗位详情行业字段

- **薪资分位条重设计**：细轨道（10px）+ 色块贴合圆角（overflow 裁剪）+ 刻度线与 P25/P50/P75 标签下移留白 + 当前值改为轨道上方菱形标记（高位绿/低位黄），岗位市场参考与公司抽屉市场数据两处统一

### 🧭 体验优化

- **hash 路由**：视图切换同步浏览器 URL（`#/jobs` `#/guide` `#/observatory` `#/industry` `#/config` `#/resume`），浏览器前进/后退可返回上一模块（视图状态保留）；带 hash 刷新直达对应视图

- **命名统一**：「市场观察台」→「市场观察」，导航分组「市场洞察」→「市场」（市场观察 = 市场维度 / 行业观察 = 行业维度 对称体系）

- **行业详情区域分布样式补齐**：区域行卡片 + 机会密度条

- **日间模式**：顶栏一键切换日/夜（全套 --gaj-\* 令牌浅色值，无需逐视图适配）；localStorage 持久化，首次访问跟随系统偏好；sticky 栏等半透明表面随主题切换

### 🧹 全面质检（代码/UI/UX 巡检）

- **修复行业详情接口含斜杠行业名 404**（如「汽车研发/制造」点击无响应）：路由改用 path 转换器

- **日间模式细节**：象限图 C 级气泡填充改用表面色（原白色 4% 透明在浅底不可见）；行业薪资条/徽章/图例确认全部走变量

- **窄屏对称性**：900px 断点下 sticky 摘要栏负边距与内容区对齐

- **死代码清理**：store.toggleView / store.marketForIndustry / store.openCompany / openDrill 的 industry 类型分支（均无调用方）

- **巡检结论**：6 视图 × 暗/日两主题 × 1440/800 两宽度全链路自动化巡检 0 控制台错误、0 横向溢出

### 🎨 口径 UI 融入全局体系

- **侧边栏口径筛选重构**：去除内联样式与 2px 粗边框，改为与分组同节奏的「数据口径」标签 + 主题化下拉（选项精简为命名、岗位数入 title），窄屏（≤900px）自动隐藏防挤爆

- **口径管理导航项统一**：移除无样式的双行结构，回归标准单行 nav-label

- **口径管理页重做**：容器复用 config-body（滚动 + 居中），表格对齐 ind-table 风格（行 hover/激活高亮 primary-muted），删除自带 .btn/.input 重复组件改用全局控件，内联样式全部收编进 scope.css

- **市场观察口径徽标**：tabs 右侧显示当前口径（切口径后数据来源一目了然）

- 修复口径管理页内容超高被裁剪（补滚动容器）

### 🐛 修复

- 公司抽屉内嵌岗位「在主视图打开」的空引用回归（closeCompany 置空顺序问题）

***

## \[v0.3.0] — 市场观察与全链路下钻

### 📈 新增：市场观察

顶部新增「市场观察」入口，把采集到的岗位数据聚合成四个视角的市场行情信号。图表全部纯 SVG 渲染，零图表库依赖：

| Tab          | 看什么                                                           |
| ------------ | ------------------------------------------------------------- |
| 💰 **薪资定价**  | 全市场分位概览（P10\~P90 / 中位数 / 均值）；经验-薪资曲线、学历-薪资柱图、行业中位数对比、融资阶段定价   |
| 🗺️ **区域热力** | 经纬度网格热力图（每格≈1.1km），支持「按岗位 / 按公司」双模式切换；热点区域 Top10（区域 × 代表行业切片） |
| 🚨 **信号雷达**  | 加班强度分布（重/中/轻/无/未知）、外包与出差占比、行业红旗率 Top8、红旗公司榜                   |
| 🔥 **技能热度**  | 技能需求榜：需求岗位数 / 招聘公司数 / 均薪 / 薪资溢价（相对市场均价）/ 头部行业                 |

### 🔍 全链路下钻（图表 → 抽屉，不离开页面）

- **一切可点的图表都能下钻**：柱图 / 折线 / 热力点 / 表格行点击后，右侧抽屉拉出对应的岗位或公司列表

- **抽屉内继续看详情**：抽屉里点岗位卡片 → 抽屉内嵌岗位详情（JD / 技能 / 福利 / 公司信息）；点公司 → 抽屉内嵌公司详情（含名下岗位）；岗位 ↔ 公司可连续跳转，全程不离开当前页面

- **公司抽屉同样内嵌**：公司图鉴、信号雷达红旗公司等处的公司抽屉里点岗位，直接在抽屉内查看，不再跳走

- 热力图「按公司」模式下钻直达区域公司列表，再逐家进岗位

### 🎯 数据口径一致性（下钻数字 = 图表数字）

市场观察全面做了口径校准，图表显示多少、抽屉里就是多少——这些数字是校准预期的依据，准确性优先：

- **「未知」值下钻不再为空**：聚合端把 NULL/空的行业 / 区域 / 加班档位显示为"未知"，下钻时自动翻译回 NULL/空值匹配（修复前"未知"行业 62 岗下钻结果为 0）

- **薪资样本口径锁定**：薪资定价柱图的计数只含有薪资样本的岗位，下钻带同口径筛选，未来出现无薪资岗位也不会错位

- **热点区域 Top10 切片化**：按「区域 × 代表行业」聚合，行内岗位数 / 公司数与下钻抽屉返回数严格相等（修复前按经纬度网格计数，与按行政区查询的下钻数字对不上）

- **热力点全区口径**：悬停提示显示所在行政区的全区岗位 / 公司数，与点击下钻后的数字一致

### 🏗️ 后端模块化重构

Web 后端按领域拆分，API 行为不变、可维护性提升：

- **Store 层**：`gaj/store/observatory.py`（市场观察聚合）、`gaj/store/companies.py`（公司图鉴查询）从 `index.py` 独立

- **路由层**：`gaj/web/routes/`（observatory / companies / config 三个 APIRouter）+ `gaj/web/runtime.py`（后台任务与日志运行时），新增视角只需追加路由模块

### 📊 市场定位与象限迁移

- **公司象限图迁移**：从公司图鉴移出（图鉴保留卡片墙/对比），改为共享组件 `core/quadrant.js`，落地两处——市场观察新增第 5 个 Tab「公司象限」（全市场公司均薪-分数分布，点击气泡开公司抽屉）、行业观察详情页新增「公司象限」区块（该行业公司分布）

- **岗位详情「市场参考」增强**：新增行业薪资分位条（P25/P50/P75 区间 + 本岗位置点 + 高/低位判定）与经验对标（岗位经验段匹配行业经验-薪资中位）

- **公司抽屉「市场数据」增强**：新增行业分位条（公司均薪定位）与规模对标（公司在招 vs 行业平均公司规模）

- **行业详情数据口径统一**：代表公司 Top8 改用与公司象限同源的数据（同一公司统计口径，有分按公司分、无分按在招数），两区块分数/集合完全一致

- **公司象限视觉统一**：市场观察「公司象限」改为区域热力同款结构（obs-chart 图表容器 + 等级图例 + 「公司象限榜 Top 10」配套表，行点击开公司抽屉）

- **行业下钻打通**：市场观察所有行业触点可进入行业观察详情——薪资/雷达行业柱图点击、技能榜行业标签、热点区域表行业按钮、下钻岗位详情行业字段

- **薪资分位条重设计**：细轨道（10px）+ 色块贴合圆角（overflow 裁剪）+ 刻度线与 P25/P50/P75 标签下移留白 + 当前值改为轨道上方菱形标记（高位绿/低位黄），岗位市场参考与公司抽屉市场数据两处统一

### 🧭 体验优化

- **hash 路由**：视图切换同步浏览器 URL（`#/jobs` `#/guide` `#/observatory` `#/industry` `#/config` `#/resume`），浏览器前进/后退可返回上一模块（视图状态保留）；带 hash 刷新直达对应视图

- **命名统一**：「市场观察台」→「市场观察」，导航分组「市场洞察」→「市场」（市场观察 = 市场维度 / 行业观察 = 行业维度 对称体系）

- **行业详情区域分布样式补齐**：区域行卡片 + 机会密度条

- **日间模式**：顶栏一键切换日/夜（全套 --gaj-\* 令牌浅色值，无需逐视图适配）；localStorage 持久化，首次访问跟随系统偏好；sticky 栏等半透明表面随主题切换

### 🧹 全面质检（代码/UI/UX 巡检）

- **修复行业详情接口含斜杠行业名 404**（如「汽车研发/制造」点击无响应）：路由改用 path 转换器

- **日间模式细节**：象限图 C 级气泡填充改用表面色（原白色 4% 透明在浅底不可见）；行业薪资条/徽章/图例确认全部走变量

- **窄屏对称性**：900px 断点下 sticky 摘要栏负边距与内容区对齐

- **死代码清理**：store.toggleView / store.marketForIndustry / store.openCompany / openDrill 的 industry 类型分支（均无调用方）

- **巡检结论**：6 视图 × 暗/日两主题 × 1440/800 两宽度全链路自动化巡检 0 控制台错误、0 横向溢出

### 🎨 口径 UI 融入全局体系

- **侧边栏口径筛选重构**：去除内联样式与 2px 粗边框，改为与分组同节奏的「数据口径」标签 + 主题化下拉（选项精简为命名、岗位数入 title），窄屏（≤900px）自动隐藏防挤爆

- **口径管理导航项统一**：移除无样式的双行结构，回归标准单行 nav-label

- **口径管理页重做**：容器复用 config-body（滚动 + 居中），表格对齐 ind-table 风格（行 hover/激活高亮 primary-muted），删除自带 .btn/.input 重复组件改用全局控件，内联样式全部收编进 scope.css

- **市场观察口径徽标**：tabs 右侧显示当前口径（切口径后数据来源一目了然）

- 修复口径管理页内容超高被裁剪（补滚动容器）

### 🐛 修复

- 通义千问驱动：修复输入框输入后无法激活发送按钮的问题

***

## \[v0.2.0]

> 代码已合入但尚未同步到 `docs/index.html` 对外文档的功能。

### 🏢 新增：公司图鉴

独立的「公司图鉴」Tab，**按公司维度**把散落的岗位聚合成一张一张的公司卡片，支持多种浏览 / 筛选 / 对比维度：

**图鉴总览**

- **进度与想去清单**：顶部实时显示「已鉴定 X / 总公司 Y 家」；想去收藏可一键切换「只看想去 / 全部」

- **三套即时榜单切换**：

  - 🏆 分数榜：公司分从高到低

  - 🔥 招聘力度榜：在招岗位数从多到少

  - 💰 薪资榜：平均月薪从高到低

- **多维筛选**：公司名搜索 + 行业（带公司数）+ 融资阶段（带公司数）

**三种浏览模式**

| 模式              | 能看到什么                                                                                  |
| --------------- | -------------------------------------------------------------------------------------- |
| **卡片墙**（默认）     | 每家公司一张卡片：排名徽章 / 公司分 / 在招岗数 / 行业·融资·规模 / 均薪·城市 / 最佳岗位（皇冠+分数）；想去收藏 / 加入对比 / 「去鉴定」解锁未开卡公司 |
| **象限气泡图**       | X=平均薪资、Y=公司分、气泡大小=在招岗位数；右上角标注「高分·高薪甜蜜点」；薪资未知的公司单独列成 chip，点击气泡直接进公司                     |
| **并排对比**（2-4 家） | 每家一列横向对比：公司分 + **四维雷达图**（财务/成长/资源/WLB）+ 关键指标表（规模/阶段/均薪/在招/最佳岗位）                        |

**公司详情抽屉**（点击卡片/气泡进入）

- **基础信息 chips**：行业、融资阶段、规模、公司性质、成立时间、日均工时、在招岗数、均薪

- **公司简介 + 经营范围**（可展开全文）

- **四维画像雷达**：基于名下所有岗位的规则分均值计算（财务/成长/资源/WLB）

- **AI 评价 · 图鉴词条**：手动触发大模型基于公司资料和在招岗位生成公司专属词条，包含：业务解读、技术栈画像、招聘紧迫度（带理由）、值不值得去（带理由）、亮点 chips、顾虑 chips、面试策略 chips、业务关键词 cloud；历史评价 append-only 保存，可回看、可单条删除

- **名下岗位列表**：直接列该公司所有采到过的岗位（薪资/城市/规则状态分/AI 次数/是否下线），点击直接跳对应岗位详情

**「去鉴定」解锁机制**

- 新公司第一次出现在图鉴里是「锁卡」状态：公司分显示 `?`、详情隐藏

- 点卡片上的「去鉴定」，系统自动挑出公司当前最佳岗位发起 AI 打分；打分完成即自动解锁这家公司，公司分、雷达、最佳岗位等信息一次性全部透出

- 公司打分自带 180 天保鲜缓存，不会频繁重评

**不会无故缺分**

- 用户主动用 AI 评过的岗位，即使被规则层淘汰，也会参与公司平均分的加权计算

- 名下岗位全部被规则淘汰时，回退到公司级 AI 评价分，保证至少有分数参考（之前像雅迪这类公司会显示"无分"，现在不会了）

### 🕷️ 采集更聪明，不会漏也不会白跑

- **续翻接力**：前几页翻不出新职位时，会自动跳到上一次采集停止的页码再往后翻几页；兼顾"前几页优先抓最新职位"和"后面旧职位不会一直采不到"

- **翻到底自动清零**：搜索结果真的翻到最后一页时，续翻锚点自动失效，下次从头开始；不会因为中途被迫中断而误清除接力页码

- **单次采集有上限**：AI 自动采集时默认最多新抓 60 个职位就停，执行时长得预期

- **近期职位不重复抓**：60 天内已采集过的职位直接跳过，节省翻页时间

### 🧠 AI 打分更省、更稳

- **公司打分有 180 天缓存**：公司情况变化慢，大半年内打过的公司分直接复用，不浪费模型调用

- **岗位打分有 90 天保鲜期**：过期自动排队重评，不会一直用过期的评分

***

## \[v0.1.0] — 初始发布版

> 对应 `docs/index.html` 官网落地页当前介绍的全部能力。
> 定位：可用的 MVP 主循环（采集 → 打分 → 分析 → 优化 → 校准）已跑通。

### 🎯 核心价值

- **坑位图鉴**：个人猎头系统，本地运行 · 零词元消耗 · Agent Loop 自我进化

- Hero 数据：`4` 评分维度 · `10` 硬性规则 · `4+` AI 大模型 · `0` 词元消耗

### 🔁 Agent Loop（每日循环，`daily` 命令一键编排）

1. **采集** — CDP 驱动 Chrome 增量浏览 BOSS 直聘，连续 3 页全重复即判定已覆盖
2. **打分** — 四维十项规则引擎即时打分，硬淘汰按置信度分级，模糊区间调 AI 介入
3. **分析** — 网页版大模型（DeepSeek / 豆包 / 通义 / Kimi）深度分析，0-10 分 + 面试建议
4. **优化** — 目标岗位 ID + 主简历，生成定制简历（optimize 微调 / rewrite 重写，实验性）
5. **校准** — 打分数据反推画像权重、薪资预期、硬性底线，5 套预设一键切换
6. **迭代** — 更新画像与规则，下一轮更精准

### 💎 四大核心能力（Features）

| 能力           | 要点                                                                           |
| ------------ | ---------------------------------------------------------------------------- |
| 自动采集 BOSS 直聘 | CDP 调试模式驱动真实 Chrome；自然访问节奏；连续整页重复降速；覆盖率 ≥90% 放慢基础节奏                          |
| 双层打分引擎       | 规则层：4 维度 × 10 项，权重画像驱动；硬淘汰 10 条，置信度 < 60% 标 REVIEW 不误杀；AI 层：10 条触发器自动调用网页大模型 |
| 针对性简历优化（实验性） | 读取 JD 全文 + 主简历，optimize / rewrite 两种风格，JD 关键词融入真实经历，不造假                      |
| 预期校准与反馈      | 5 套权重预设（平衡 / 成长 / 财务 / 生活 / 资源优先），数据反向指导调整策略                                 |

### 📊 打分体系（Scoring System）

**维度 × 权重**

- 财务回报 Finance × 30%（薪资下限/中位数/上限惊喜/股权激励/高价值福利/构成质量）

- 职业发展 Growth × 30%（技术栈重合/公司阶段/业务方向/团队规模/经验学历/技术深度/前瞻性）

- 平台与文化 Resource × 10%（城市匹配/行业重叠/体量偏好/特殊资源/文化适配）

- 工作生活平衡 WLB × 30%（工作模式/弹性福利/企业规范/加班强度/通勤便利）

**硬性淘汰（10 条，FATAL / REVIEW 两级）**

| 编号   | 规则              | 等级     |
| ---- | --------------- | ------ |
| H-01 | 城市不可接受          | FATAL  |
| H-02 | 行业被拒绝           | FATAL  |
| H-03 | 公司规模过小          | FATAL  |
| H-04 | 薪资低于硬性下限        | FATAL  |
| H-05 | 出差强度超标          | REVIEW |
| H-06 | 加班强度超标（996/大小周） | REVIEW |
| H-07 | 命中排除关键词黑名单      | FATAL  |
| H-08 | 外包 / 驻场岗位       | REVIEW |
| H-09 | 学历硬性不符          | FATAL  |
| H-10 | 经验年限硬性不符        | REVIEW |

**AI 介入触发器（10 条）**
WLB 偏低偏高需核实 · 行业语义相关需判断 · 总分 6.0-7.5 模糊区间 · JD 内容过短 · 硬规则命中证据不足 · 匿名雇主推测 · 文化适配存疑 · 公司稳定性风险 · 用户主动深度分析（共 10 类场景）

### 👥 两种使用方式（Dual Mode）

| 维度   | Skill + AI 驱动（For Everyone）                      | CLI + Web 图鉴（For Developers）        |
| ---- | ------------------------------------------------ | ----------------------------------- |
| 上手门槛 | 4 步：安装 skill → 复制 BOSS 筛选 URL → 自然语言发指令 → AI 全自动 | 需 Python，`python3 -m gaj <cmd>` 全可控 |
| 交互形态 | 跟 AI 说句话："帮我看看今天有什么新职位"                          | 命令行 + 浏览器访问 `127.0.0.1:8765`        |
| 词元消耗 | 消耗 AI 助手词元                                       | 零词元消耗（网页大模型免费额度）                    |
| 定制能力 | AI 自主编排                                          | 代码完全开放，前后端热重载，任意 AI Agent 可自定义页面    |

### ⌨️ CLI 命令（6 个核心）

| 命令                       | 场景                | 典型用法                                                  |
| ------------------------ | ----------------- | ----------------------------------------------------- |
| `crawl`                  | 从 BOSS 列表页 URL 采集 | `gaj crawl "https://zhipin.com/..." [--max-pages N]`  |
| `score`                  | 规则批量打分            | `gaj score --all` / `gaj score --job JOB_ID`          |
| `ai-score`               | 网页版大模型深度分析        | `gaj ai-score --job JOB_ID --provider deepseek`       |
| `resume`                 | 定制岗位简历（实验性）       | `gaj resume --job JOB_ID [--style optimize\|rewrite]` |
| `web`                    | 启动 Web 图鉴（含热重载）   | `gaj web [--port 8765] [--no-reload]`                 |
| `setup-chrome` / `check` | CDP 环境准备与健康检查     | `gaj setup-chrome` / `gaj check`                      |

### 🔌 Agent JSON 接口

所有命令走 `python3 -m gaj agent <subcmd>`，stdout 严格只输出一个 JSON 信封，`exit_code` 决策：

| 子命令       | 用途                                  |
| --------- | ----------------------------------- |
| `status`  | 健康检查 + 数据概况（任何流程第一步）                |
| `jobs`    | 查询职位列表（筛选 / 排序 / 分页）                |
| `job`     | 单职位详情（JD 全文 + 规则分 + AI 分）           |
| `analyze` | AI 打分（`--job` 单个 / `--auto` 批量挑候选）  |
| `crawl`   | 增量采集（自动降速、覆盖提前结束）                   |
| `daily`   | 每日编排：采集 → 挑候选 → AI 分析 → Markdown 摘要 |

**容错保障**：最外层异常兜底，任何失败都返回 JSON，不会静默挂起；数据逐个职位增量落盘，超时 kill 后重跑不重复。

### 🏗️ 技术架构

纯 Python 实现，CDP 驱动真实 Chrome，全链路本地存储，零云端依赖：

```
Chrome CDP (9222)
  → BOSS 爬虫 (boss_scraper)
    → 规则引擎 (gaj/core)
      → AI 分析 (gaj/ai, 4+ 网页版大模型)
        → 本地存储 (data/, 已 gitignore)
          → Web 图鉴 (FastAPI + Alpine.js)
```

**架构要点**

- 数据隐私：所有数据 `data/` 下本地落盘，gitignore 排除

- 多模型支持：DeepSeek / 豆包 / 通义千问 / Kimi，网页版驱动不消耗 API Key

- 自然节奏浏览：连续重复页拉长翻页间隔，覆盖率高时放慢基础节奏

- 代码开放 · 热重载：前后端支持 hot reload，可用任意 AI Agent 自定义

